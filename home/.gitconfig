[include]
    path = ~/.gitconfig.local
[user]
    name = izumin5210
    email = masayuki@izumin.info
[core]
    autocrlf = input
    editor = $EDITOR
    excludesfile = ~/.gitignore_global
[color]
    ui = true
[github]
    user = izumin5210
[alias]
    co = checkout
    cb = checkout -b
    st = !git_st
    sta = !git_sta
    std = !git_std
    str = !git_str
    ste = !git_ste
    logg = log --stat --pretty=format:'%C(yellow)%h %C(cyan)%ar %C(magenta)%an%n%C(white)%s %Cred%d'
    graph = log --graph --decorate --oneline
    cv = commit -v
    ca = commit --amend
    cam = commit --amend -m
    ic = commit --allow-empty -m 'Initial commit'
    rbm = rebase master
    mm = merge master
    wip-commit = !git_wip_commit
    wc = wip-commit
    create-repo = !git_create_repo
    branch-deletable = !git branch --merged | grep -v master | grep -v '*'
    branch-deletable-origin = !git branch --remotes --merged | grep origin/ | grep -v master | sed s~origin/~:~
    clean-branch = !git branch -d $(git branch-deletable)
    clean-origin-branch = !git push origin $(git branch-deletable-origin)
    sync = !git checkout master && git pull origin master && git fetch --prune origin && git clean-branch && git clean-origin-branch
    pr = !hub browse -- compare/$(git symbolic-ref --short HEAD)'?'expand=1'&'body=%23%23%20WHY%0A%0A%23%23%20WHAT%0A%0A%23%23%20REF%0A%0A
[push]
    default = current
[filter "lfs"]
    clean = git lfs clean %f
    smudge = git lfs smudge %f
    required = true
[ghq]
    root = ~/src
[secrets]
    providers = git secrets --aws-provider
    patterns = [A-Z0-9]{20}
    patterns = (\"|')?(AWS|aws|Aws)?_?(SECRET|secret|Secret)?_?(ACCESS|access|Access)?_?(KEY|key|Key)(\"|')?\\s*(:|=>|=)\\s*(\"|')?[A-Za-z0-9/\\+=]{40}(\"|')?
    patterns = (\"|')?(AWS|aws|Aws)?_?(ACCOUNT|account|Account)_?(ID|id|Id)?(\"|')?\\s*(:|=>|=)\\s*(\"|')?[0-9]{4}\\-?[0-9]{4}\\-?[0-9]{4}(\"|')?
    allowed = AKIAIOSFODNN7EXAMPLE
    allowed = wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
[commit]
    template = ~/.gitcommit-template
